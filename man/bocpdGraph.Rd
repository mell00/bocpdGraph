% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bocpdGraph.R
\name{bocpdGraph}
\alias{bocpdGraph}
\title{BOCPD on Graph-Structured Multivariate Series}
\usage{
bocpdGraph(data, graph, model, hazard, control = list(), ...)
}
\arguments{
\item{data}{Node-by-time observations. Accepts:
\itemize{
\item a numeric matrix with rows = nodes and cols = time,
\item a list of length n_nodes where each element is a numeric vector of length T.
}}

\item{graph}{Graph structure. Accepts:
\itemize{
\item a square adjacency matrix (0/1 or weighted),
\item an \code{igraph} object (if igraph is installed).
}}

\item{model}{A graph-aware model object. Must be compatible with:
\code{graph_model_init()}, \code{graph_pred_loglik()}, \code{graph_model_update()}.}

\item{hazard}{A hazard function used by \code{update_run_length()}.}

\item{control}{Optional list of controls. Uses \code{r_max} (default 200),
\code{store_R} (default TRUE), \code{verbose} (default FALSE).}

\item{...}{Additional arguments forwarded to the hazard via \code{hazard_args}.}
}
\value{
An object of class \code{"bocpdGraph"}.
}
\description{
Runs Bayesian online changepoint detection (BOCPD) for data observed on
nodes of a graph. The BOCPD recursion is applied per node; graph structure
enters only through the graph-aware model's predictive log-likelihood.
}
