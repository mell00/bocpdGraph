% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazard.R
\name{hazard_from_pmf}
\alias{hazard_from_pmf}
\title{Discrete hazard from a segment-length PMF}
\usage{
hazard_from_pmf(pmf, tail_hazard = 1)
}
\arguments{
\item{pmf}{Numeric vector of nonnegative values (will be normalized).
Interpreted as probabilities for lengths 1..K.}

\item{tail_hazard}{Hazard used for run lengths r >= K (default 1, i.e. force a changepoint).}
}
\value{
A hazard function depending on \code{run_length}.
}
\description{
Converts a discrete segment length distribution into a hazard.
For a segment length L with PMF \code{pmf[k] = P(L = k)} for k=1..K,
the hazard at run length r (0-indexed) is:
\deqn{h(r) = P(L = r+1 \mid L \ge r+1) = pmf[r+1] / S[r+1],}
where \eqn{S[j] = P(L \ge j)}.
}
\examples{
# Length is uniform on {1,2,3}
h <- hazard_from_pmf(c(1, 1, 1))
h(0:5, t = 1)

}
